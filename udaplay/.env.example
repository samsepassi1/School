# Copy to .env and fill in. Both keys are optional — the project degrades
# gracefully when keys are absent (default sentence-transformer embeddings,
# heuristic evaluator, no web search).

OPENAI_API_KEY=
TAVILY_API_KEY=
